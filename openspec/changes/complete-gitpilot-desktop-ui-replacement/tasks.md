## 1. 冻结基线与保护业务契约

- [x] 1.1 记录当前工作区已有改动，划分本变更允许修改的 Desktop 文件，避免覆盖无关改动
- [x] 1.2 在 1100×720、1440×900、800×500 原生 Tauri 窗口保存当前 UI 截图和面板尺寸（CUA 截图实际为 1103×723、1428×894、788×494 的客户区尺寸）
- [x] 1.3 记录当前 JS/CSS bundle、Radix 依赖、CSS 行数、旧 token 引用和 `!important` 数量
- [x] 1.4 为 App 单次连接/断开、全局快捷键和窗口隐藏语义补充契约测试（连接/断开与快捷键由 session/workbench 契约测试覆盖；原生 CloseRequested 隐藏已验证，托盘点击恢复仍待系统托盘级验收）
- [x] 1.5 为项目展开、新建项目任务、新建独立任务、会话切换和 cwd 语义补充测试
- [x] 1.6 为聊天初始定位、流式跟随、输入命中区和执行面板折叠状态补充回归测试

## 2. 建立真正的目标 UI 基础

- [x] 2.1 新建 `components/desktop`、`components/workbench`、`components/features` 目录和明确的公共导出边界
- [x] 2.2 把 `App` 收敛为生命周期控制器与单一目标工作台入口，建立真实独立的目标渲染树
- [x] 2.3 统一 `tokens.css` 为唯一语义 token，并列出待删除的旧 `--color-*` alias
- [x] 2.4 将 `index.css` 收敛为入口文件，建立 UI primitive 与 feature CSS Modules 的样式责任规则
- [x] 2.5 增加 lint/校验脚本，阻止 feature 新增旧 token、全局业务选择器和未登记的 `!important`
- [x] 2.6 建立开发态 Desktop UI Gallery，覆盖基础原语、长文本、焦点、状态和 reduced-motion
- [x] 2.7 记录目标 Graphite Workbench 的颜色、字体、间距、行高、面板和交互状态基线

## 3. 重建标题栏与工作台壳

- [x] 3.1 使用新目录和局部样式重建标题栏，保持拖动、窗口按钮、账户入口和左右面板控制
- [x] 3.2 引入目标工作台单一 resizable panel 实现，实现左右面板拖动、键盘调整、边界夹取和布局持久化
- [x] 3.3 重建中心工作区、底部 Tabs 面板和状态栏，分别表达平台、sidecar、Agent 和终端状态
- [x] 3.4 实现 800px 最小窗口的导航与执行 Sheet 降级，不重复挂载业务区域
- [x] 3.5 删除旧壳层对应的手工 pointer/Grid 双重控制和已替换全局选择器
- [x] 3.6 在三种窗口尺寸运行壳层截图与键鼠验收，并完成首个视觉确认门槛（1100/1440/800 三档均完成原生截图，验证面板折叠、输入和终端）

## 4. 重建项目任务导航与账户交互

- [x] 4.1 以固定列网格重建项目行：展开控件、类型图标、可截断名称和固定操作区
- [x] 4.2 重建项目任务和独立任务列表，保证超长名称省略、完整提示、选中态和滚动边界
- [x] 4.3 保持项目行悬浮创建任务、独立任务创建、移除项目和会话切换 action 不变
- [x] 4.4 重建账户菜单和 DesktopContextMenu，验证键盘、焦点返回、窗口边界和剪贴板降级（账户菜单、任务右键菜单、Esc 返回和复制会话路径均已原生验证）
- [x] 4.5 删除侧栏、账户和右键菜单的旧 DOM 类名与旧全局选择器
- [x] 4.6 在三种窗口尺寸验证空列表、超长项目名、超长任务名和 50+ 任务场景（UI Gallery 固定注入 60 个任务、超长项目/任务名；1100×720、1440×900、800×500 原生窗口截图已完成；空列表沿用基线原生验收记录）

## 5. 重建对话与输入工作流

- [x] 5.1 在目标中心工作区隔离 ChatView/输入器阅读区域与视觉边界，MessageBubble 的 DOM 仍待下一阶段拆分
- [x] 5.2 重建 ConversationTimeline 和 ExecutionActivity，统一定位、预览和真实工具批次表达
- [x] 5.3 重建输入器、附件、拖放、发送与停止控件，保持透明命中区和滚动条可点击
- [x] 5.4 重建模型、思维级别、slash 命令和全局命令面板，保持提交值与快捷键优先级
- [x] 5.5 验证长正文、代码块、表格、大附件列表、连续工具批次和模型长名称（UI Gallery 固定注入 Markdown 长正文/代码块/表格、60 项附件、13 步连续工具批次和长模型名；三档原生窗口截图已完成）
- [x] 5.6 删除对话、输入器和命令区域的旧全局选择器，并通过滚动回归测试

## 6. 重建执行、终端与弹层

- [x] 6.1 重建执行步骤、阶段状态和详情面板，保持真实工具生命周期和原始输出
- [x] 6.2 重建底部输出与终端壳，保持 xterm 生命周期、当前目录和面板状态
- [x] 6.3 重建登录、断线、错误、Extension Dialog 和 Sheet，统一 overlay、Esc 和焦点返回
- [x] 6.4 验证执行面板关闭后可恢复、终端切换、扩展响应和窗口缩放行为（原生验证执行面板、终端关闭/恢复、800px 双 Sheet 与 Esc）
- [x] 6.5 删除执行、终端和弹层区域的旧全局选择器与重复 Portal 实现

## 7. 删除混合迁移遗留

- [x] 7.1 反向搜索并删除 Legacy 渲染树、`VITE_TAURI_DESKTOP_UI_V2` 和兼容根 class
- [x] 7.2 删除 `legacy.css`、旧 `--color-*` alias 和 `index.css` 中已替换的业务选择器
- [x] 7.3 将第三方 xterm 覆盖隔离到独立样式文件，并清除其余无依据 `!important`
- [x] 7.4 删除假的或未使用的 UI 包装组件，清理未使用 Radix/shadcn 依赖
- [x] 7.5 对比替换前后 CSS 行数、token 引用、bundle 和依赖，确认维护复杂度实际下降
- [x] 7.6 更新 `docs/design-docs/` 专题设计、设计索引和 `docs/architecture.md`

## 8. 完整原生验收与交付

- [x] 8.1 运行 `npm.cmd run test` 和 `npm.cmd run build`
- [x] 8.2 运行 `cargo check`、相关 Rust 测试、编码检查和 `git diff --check`
- [x] 8.3 在 1100×720、1440×900、800×500 完成最终原生截图对比（CUA 客户区分别为 1103×723、1428×894、788×494）
- [x] 8.4 验收标题栏/托盘、项目任务、滚动、输入、附件、模型、执行、弹层和终端全链路（项目任务、滚动、输入、附件、模型、执行、Sheet、终端以及 50+ 任务/连续工具批次 Gallery 场景已验收；标题栏、托盘隐藏/恢复和关闭语义已完成原生验证）
- [x] 8.5 构建 MSI 与 NSIS，并验证安装态目录选择、sidecar 启动、窗口控制和托盘恢复（MSI/NSIS 构建、MSI 资源提取、提取目录 sidecar 启动、原生文件夹选择、窗口控制和托盘恢复均已完成验证）
- [x] 8.6 将所有失败项闭环后，确认旧 UI 和旧样式已完全删除，再标记替换完成
